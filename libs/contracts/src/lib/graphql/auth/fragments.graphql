# ================================================================
# AUTH FRAGMENTS
# ================================================================

fragment UserFields on User {
  id
  email
  name
  role
  phone
  emailVerified
  createdAt
  updatedAt
}

fragment AuthPayloadFields on AuthPayload {
  accessToken
  user {
    ...UserFields
  }
}
