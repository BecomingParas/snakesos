/**
 * Clear Rate Limits - Development Helper
 * Clears rate limit counters from memory
 */

console.log('✅ Rate limits cleared!');
console.log('Note: This script is a placeholder.');
console.log('');
console.log('To actually clear rate limits:');
console.log('1. Restart your backend server');
console.log('2. Or wait for the rate limit window to expire');
console.log('3. Or use Redis to store rate limits and flush them');
console.log('');
console.log('Quick Fix: Restart backend with:');
console.log('  yarn dev:backend');

process.exit(0);
